import Vue from 'vue'
import Vuex from 'vuex'

// eslint-disable-next-line camelcase
import login_checks from '../modules/login_checks'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    login_checks
  }
})
